# Git Configuration

Modular Git configuration and aliases.

## Setup (One-shot)

Run once to link this configuration to your global Git config:

```bash
./4-git/setup.sh
```

## Usage

A compact, colored graph view of all branches and tags:

```bash
git tree
```

Any new aliases added to `4-git/gitconfig` will be available immediately after the initial setup.
